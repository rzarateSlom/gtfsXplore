# Cargado automáticamente por `vite build` (modo producción). No es secreto — se commitea.
# /gtfs lo resuelve server.js en producción (mismo rol que el proxy de vite.config.ts en dev).
VITE_API_BASE=/gtfs/GtfsExposeAPI
VITE_API_GUID=
